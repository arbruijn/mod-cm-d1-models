olmod addon to replace CM bot models by D1 models

## How to run

Place the `mod-cm-d1-models.dll` file in your olmod directory. Also add the D1
`descent.hog` and `descent.pig` file to this directory.
